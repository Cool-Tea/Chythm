#include "Globals.h"

Application app = {
    .win = NULL,
    .ren = NULL,
    .font = NULL,
    .is_running = 1,
    .error_level = 0,
    .is_loaded = 0,
    // .key_status = NULL
    .mutex = NULL
};